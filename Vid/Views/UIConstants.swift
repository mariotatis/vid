import SwiftUI

// Shared UI constants to keep spacing consistent across views
let TOP_NAV_CONTENT_INSET: CGFloat = 60 // Space reserved below TopNavigationBar
let VIDEO_ROW_VERTICAL_PADDING: CGFloat = 12 // Vertical padding for video rows
let LIST_ROW_HORIZONTAL_INSET: CGFloat = 16 // Leading/trailing inset for list rows
// Small adjustment to align Playlists index list with Library/Details on iOS 15 tables
let PLAYLISTS_LIST_TOP_EXTRA: CGFloat = 60
